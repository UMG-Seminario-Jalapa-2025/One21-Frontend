apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "one21-frontend.fullname" . }}-config
  labels:
    {{- include "one21-frontend.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $value := .Values.envVars }}
  {{ $key }}: {{ $value }}
{{- end }}
